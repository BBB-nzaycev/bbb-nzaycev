name: 'Pushed'

on: 
  push:
    branches-ignore:
      - main

jobs:
  test-action:
    runs-on: ubuntu-latest
    steps:
    - name: Check out repository code
      uses: actions/checkout@v2
    - name: Check files
      run: |
        ls -la
        echo "lalala"
